# TankMark Developer Guide (v0.31)

## Project Structure

### Entry Point
- **TankMark.lua**: Event handlers, slash commands, `TankMark.Locals` localization table, key-binding handlers, and startup initialization. All WoW API references are cached here into `TankMark.Locals` so other files never call globals directly.

### Core/
Contains business logic (marking algorithms, death handling, scanner, etc.)

- **TankMark_Session.lua**: Centralized runtime state variables (`usedIcons`, `activeGUIDs`, `activeMobNames`, `disabledMarks`, `sessionAssignments`, etc.), the `MarkInfo` constants table, and **[v0.28]** `CCAuraSet` — the flattened set of parked-CC aura IDs (built at load from the grouped `CC_SOURCE` table) that `IsMarkCCd` reads. Loaded first among Core modules. **[v0.31]** `MarkNormals` no longer lives here — it moved to a persisted per-character field (`TankMarkCharConfig.markNormals`, read via `TankMark:MarkNormalsEnabled()`, default-true); the runtime default was removed.
- **TankMark_Reservation.lua**: **[v0.31]** The **reservation** seam — `Reservation.Reserve(icon, player)`, the single writer of a *manual* mark-slot claim (`/tmark assign`, the HUD). It occupies the icon (`usedIcons`) and binds the responsible player (`sessionAssignments`) in one guarded write, so `usedIcons` now has exactly two named writers: the **Ledger** (ownership — a live mob wears the mark) and this module (reservation). Pure Core (no WoW API / UI — callers keep their own HUD refresh); loaded right after the Ledger. Distinct from **ownership** and **assignment** — see `CONTEXT.md`. (The dead `AssignCC` wrapper was removed when this landed.)
- **TankMark_ZoneView.lua**: **[v0.31]** Builds the **active zone view** (`activeDB`) — `ZoneView.Merge(userZone, defaultsZone)` overlays user entries over the shipped defaults (user wins, defaults fill gaps) and runs every entry through `ZoneView.ValidateEntry` on the way in. Validation is **fail-closed on the required fields** (a non-numeric `prio`, or an empty / non-numeric / out-of-`[0,8]` `marks` array, **drops** the entry → its mob falls to the unknown-mob path; a rank is never fabricated) and **normalizing on the optional ones** (bad `type` → `KILL`, bad mob `role` → nil, non-string `class` → nil; `creatureType`/`tier` pass through untouched). **Non-mutating**: a clean entry returns the **same reference** (so `activeDB` keeps sharing tables with `TankMarkDB.Zones` exactly as before), a normalized one a shallow copy — the authored DB is never rewritten. Pure Core (no WoW API / globals — the `Data.lua` `LoadZoneData` shell reads the world and assigns `activeDB`); loaded right after the Reservation seam. **Behavior-identical**: every writer already sanitizes (editor coerces `prio`, `SyncCodec` rejects bad `prio`/`marks`, recorder hard-codes, Defaults are clean), so this is a **fail-closed chokepoint** (architecture candidate C) whose one net-new guarantee is `type` / mob-`role` enum membership — asserted nowhere else. The recorder's live `activeDB` inject (`RecordUnit`) routes through `ValidateEntry` too, so "every `activeDB` entry passed validation" holds for both doors. Harness-tested (`tests/zone_merge_spec.lua`). See **active zone view** in `CONTEXT.md`.
- **TankMark_Permissions.lua**: `CanAutomate()`, `HasPermissions()`, `ShouldDriveMarks()`, `Driver_GetGUID()`, and `Driver_ApplyMark()`. The last wraps `SetRaidTarget` with debug logging (guarded by `TankMark.DebugEnabled`). **[v0.29, swarm slice 3, PR #72]** `ShouldDriveMarks()` is the **marking** gate, deliberately distinct from `CanAutomate()` the **candidacy** gate: `CanAutomate() and (not Swarm.IsRunning() or Swarm.selfAmQueen)`, **fail-open** when the election shell isn't running. Every world-mark path reads it; `CanAutomate` is left unchanged so the swarm election/failover pool is preserved. See **Single-Marker Enforcement** under Key Systems.
- **TankMark_Assignment.lua**: Mark assignment algorithms and player detection:
  - `GetFreeTankIcon()` — iterates Team Profile entries, uses `IsMarkBusy()` to check availability.
  - **[v0.30, marking Phase 2]** legal-CC authority — the single Core source of CC capability, shared by the editor menus and the runtime resolver:
    - `TankMark.CCMap` — race-free `creatureType → {legal classes}` table (Humanoid/Beast +Shaman; Humanoid −Priest; Druid/Rogue kept). `IsLegalCC(class, creatureType)` is the creature-legality predicate over it (pure).
    - `CCRaceEligible(class, race)` — the narrow player-capability gate (only non-Troll Shamans fail Hex). **Not** `IsPlayerCCClass`, which excludes Druid/Rogue and stays role-inference-only.
    - `SelectCCSlot(authoredClass, creatureType, slots)` — the pure two-pass resolver: authored-iff-legal → first-legal in profile order; degrades to authored-class-only when creatureType is unknown; excludes used/disabled/dead/race-ineligible slots. Drives the off-client harness (`tests/legal_cc_spec.lua`).
    - `GetCCSlots()` — live enumerator of `role=="CC"` profile slots as pure data records `{mark, class, race, alive, used, disabled}`. **Replaces `FindCCPlayerForClass` (removed)** — its single-class match + availability checks are now `SelectCCSlot`'s job.
  - **[v0.30, marking Phase 3]** `RoleTierPrio(role, tier)` — pure, **total** mob-`role` × `tier` → default kill-priority lookup (HEALER 2/1/1/1 · CASTER 3/2/2/1 · MELEE 5/4/3/1, from `docs/marking-redesign/DATA-MODEL.md §5`). nil/unknown role → MELEE row, nil/unknown tier → `normal` column, so any input returns a number. **Authoring-time only:** it sits behind the editor's Role dropdown to *pre-fill* the prio field; the human always overrides and there is **no runtime recompute** (the scanner keeps reading a plain authored `prio`, so the mechanism suites are untouched). Drives the off-client harness (`tests/role_prio_spec.lua`). NB: mob `role` (HEALER/CASTER/MELEE) is a different axis from the profile `role` (TANK/CC).
  - **[v0.30, marking Phase 4]** CC-model pure helpers (`docs/adr/0002`) — `prio` drives CC-vs-kill selection; **worthiness is only a candidacy floor**:
    - `CCWorthiness(role, tier)` — the role×tier worthiness curve (HEALER 90/100 · CASTER 40/70 · MELEE 10/30; boss/rare via `ROLE_TIER_BUCKET`). Total.
    - `MeetsAutoCCFloor(role, tier)` — `CCWorthiness ≥ SCANNER_CC_FLOOR` (70): the auto-CC candidacy gate (healer any tier + elite+ casters; melee never). Shared by the scanner `ResolveCC` **and** the batch `DecidePull`; authored `type=="CC"` overrides it. Drives `tests/cc_worthiness_spec.lua`.
    - `CCTierEligible(tier)` — the CC-immune-tier gate: only `normal`/`elite` are CC-eligible (rare/rareelite/worldboss/boss are immune). Applied **universally** in `ResolveCC` (authored + auto) before `SelectCCSlot`.
    - `IsCCSlotMark(icon, profileList)` — pure: true when `icon` is a `role=="CC"` profile slot. A mob holding a CC-role mark is *parked, not killed*, so it must never block skull. Drives `tests/legal_cc_spec.lua`.
  - `GetBlockingMarkInfo()` — [v0.26] finds the highest-priority non-skull mark holder (liveness-checked via SuperWoW `mark` unit tokens). Used by the Governor Check. **[v0.28]** its private `UpdateBest` chokepoint excludes parked/CC'd holders via `IsMarkCCd()`, so a sheeped incumbent no longer blocks skull (roadmap #3 sheep-edge, PR #47). **[v0.30, Phase 4]** the same chokepoint also excludes CC-*slot* holders via `IsCCSlotMark()` — a mob on a CC-role mark never blocks skull, aura or not (fixes the "auto-CC'd mob suppresses the pack's skull" bug); reads the zone profile once at the top so the `UpdateBest` closure can see it.
  - `FindEmergencyCandidate()` — [v0.26] scans `visibleTargets` for the best skull candidate. Rejects mobs whose DB mark is explicitly not skull. **[v0.31]** exposed as the `findEmergencyCandidate` board port — its body stays a live scan, opaque to the pure `DecideSkullSuccessor` (which consumes only its `(guid, prio)` result).
  - `IncumbencyBlocks(myPrio, blockIcon, blockPrio)` — [v0.28] the single source of the skull incumbency comparison (`blockIcon and myPrio >= blockPrio`). Pure — callers fetch `GetBlockingMarkInfo()` and pass the blocker in. Shared by the decide-path governor (`GovernorBlocks`) and the death-path review (`ReviewSkullState`) so the `>=` operator can't drift between them (roadmap #3).
  - `IsMarkCCd(icon)` — [v0.28] true when the mob holding `icon` wears a parked-CC debuff (Polymorph/Sap/Shackle/Banish/Hibernate/Freezing-Trap/Wyvern-Sting/Seduction). Polls `UnitDebuff` on the SuperWoW `mark` token — the aura id is the **4th** return — against `Session.lua`'s `CCAuraSet`. Used by the `UpdateBest` early-out in `GetBlockingMarkInfo` so a parked incumbent is not a skull-blocker (roadmap #3 sheep-edge, PR #47). Fail-safe: an unrecognized/stale debuff → not-CC → blocks as before.
  - `GetUnitIDForName()` — resolves a player name to a unit ID.
  - `IsPlayerAliveAndInRaid()`, `IsPlayerCCClass()`, `GetAssigneeForMark()`.
- **TankMark_Processor.lua**: Core marking decision logic. **[v0.28] split into decide / apply** (roadmap #2):
  - `LiveBoard` — **[v0.28, Tier 2]** the **ports board**: the one seam the pure decision layer (`DecideMark` and below) reads the world through — a table of closures over the live `TankMark` methods (`playerInCombat`, `guidInCombat`, `isMarkBusy`, `markOwnerPriority`, `getFreeTankIcon`, `getBlockingMarkInfo`, `isDisabled`, **[v0.30]** the legal-CC read ports `creatureType` + `getCCSlots`, and **[v0.30, Phase 4]** the pull/scanner ports `tier` + `getTankRoster` (batch `DecidePull`) and `autoCCEnabled` (the `/tmark autocc` toggle, so `ResolveCC` stays zero-global), and **[v0.31]** `findEmergencyCandidate` (the death-path skull-candidate scan consumed by `DecideSkullSuccessor`)) plus one side-effect **sink**, `logDecision` (the guarded `DECIDE` log; a no-op under test — the death-path decider instead logs shell-side, so it needs no sink port). Built once at load; closures resolve at **call time**, so Ledger reads stay **live across a scanner tick** (candidate #1's `Assign` is visible to candidate #2's `getFreeTankIcon` — a frozen snapshot could not). Production wires `TankMark.LiveBoard`; the off-client tests inject a mock, so the decide functions touch **no** WoW/Ledger/session global directly (zero-global apart from the pure-language `L._tgetn`). Pure predicates (`IncumbencyBlocks()`) are NOT ports — they read no state and stay direct calls. See **Decision Layer** under Key Systems and the **Testing** section.
  - `ProcessUnit()` — main entry point. Validates unit, checks current mark with server-side ownership verification (SuperWoW), handles stale `activeGUIDs` invalidation, then calls `DecideMark()` + `ApplyMarkIntent()`.
  - `DecideMark(mobData, guid, mode, board)` — [v0.28] single decision entry point. Routes by `mobData` (`nil` → unknown path), emits the one `DECIDE` debug log through `board.logDecision`, and returns an inspectable intent `{ icon, reason, wasBusy?, override? }`. Applies NOTHING. `board` is the injected ports table — `TankMark.LiveBoard` in production, a mock under test.
  - `DecideKnownMark(mobData, guid, mode, board)` — [v0.28] known-mob decision: sequential/zero bails → SCANNER combat gate → `ResolveCC` → primary-mark selection (with selection-time skull theft) → free-icon fallback → governor. Returns an intent. **[v0.30, Phase 4]** tags the intent `reason="cc"` when the icon came from the CC seam (authored or auto); the sequential bail is load-bearing above the `ResolveCC` call (a `marks>1` mob must never reach the CC seam).
  - `DecideUnknownMark(guid, mode, board)` — [v0.28] unknown-mob decision (prio 5): highest free tank icon, skull only when genuinely free, never steals. Returns an intent. (No `mobData` — the unknown path has none.)
  - `DecideSkullSuccessor(snapshot, board)` — **[v0.31]** the pure **death-path** skull decision — the mirror of `DecideMark` for skull succession (architecture candidate A, PR #119). A cheap skull-slot `snapshot` (`skullAlive` / `skullLiveGUID` / `memoryOwner` / `isSequential`) in + the lazy live-scan board ports (`getBlockingMarkInfo`, `findEmergencyCandidate` — opaque, called only *past* the guards so the every-tick hot-path short-circuits) → a **tagged intent** `{ action = "none" | "adopt" | "assign", ... }`; applies NOTHING. Guard sequence: mark8-alive arm (owned→none / unowned→adopt / stale→none) → dup-event guard → sequential guard → candidate scan → incumbency. Reuses the shared `IncumbencyBlocks()` so the `>=` can't drift from `GovernorBlocks` (now pinned on the death path too). The `ReviewSkullState` shell dispatches: `adopt` → ledger-only `RegisterMarkUsage` (no re-`SetRaidTarget`), `assign` → `ApplyMarkIntent`. Drives `tests/decide_skull_successor_spec.lua`.
  - `ResolveCC(mobData, guid, board)` — [v0.28] CC resolver seam; returns the CC mark icon or nil. **[v0.30, Phase 2]** resolves creatureType **live-first** (`board.creatureType(guid)` → stored `mobData.creatureType` fallback, no write-back) and routes via the pure `SelectCCSlot` over `board.getCCSlots()`. A `nil` class routes on creatureType alone. **[v0.30, Phase 4]** resolves CC from **two** sources: authored `type=="CC"` (honored, `mobData.class` preferred) and auto-CC of an above-floor mob (`MeetsAutoCCFloor`, `authoredClass=nil` → first-legal) behind the `board.autoCCEnabled()` toggle. Gated by a **universal** `CCTierEligible` (both branches) and **reserve-a-kill-target** — no CC until a skull is committed (`board.isMarkBusy(8)`), so the first mob engaged is a kill and a lone mob (even authored `type=="CC"`) falls through to its own kill mark (BD3-A / ADR 0002).
  - `AutoCCEnabled()` — **[v0.30, Phase 4]** reads the `/tmark autocc` toggle (`TankMarkCharConfig.autoCC`, default off); injected into the decision layer via the `autoCCEnabled` board port so `ResolveCC` never touches the global.
  - `MarkNormalsEnabled()` — **[v0.31]** reads the `/tmark normals` toggle, now **persisted per-character** (`TankMarkCharConfig.markNormals`) — it was a Session runtime field that reset every reload, so an explicit off never survived. **Default-true** (`markNormals ~= false`): an unset config keeps marking normals; only an explicit off persists. Read directly by `ProcessUnit` + the Batch filter (not board-injected — it gates the pre-decision sanity filter, not the decision layer). Drives `tests/mark_normals_spec.lua`.
  - `GovernorBlocks(icon, myPrio, mode, allowSteal, board)` — [v0.28] shared skull-governor gate. `allowSteal` freezes the prio-5 asymmetry (known=true may steal an occupied skull; unknown=false never does). The skull-free incumbency check routes through the shared `IncumbencyBlocks()` predicate (roadmap #3). Returns a block-reason string or nil.
  - `ApplyMarkIntent(guid, name, intent, skipProfileLookup)` — [v0.28] sole decide-path apply edge: `RegisterMarkUsage` (Ledger record) then `Driver_ApplyMark`. (Batch's within-sequence `marks>1` cursor still applies directly; bodies *beyond* the sequence route through this edge via `DecideUnknownMark` — the cursor clamps instead of wrapping, PR #45.)
  - `IsMarkBusy(iconID)` — [v0.26] checks `MarkMemory`, SuperWoW `mark` units, and `usedIcons`. Debug logging guarded by `TankMark.DebugEnabled`.
  - `GetMarkOwnerPriority(iconID)` — [v0.26] resolves the priority of the current mark holder.
  - `RegisterMarkUsage()`, `RecordUnit()`.
- **TankMark_Death.lua**: Death detection, mark cleanup, and skull priority management:
  - `HandleCombatLog()` / `HandleDeath()` — resolve dead mob GUID before eviction.
  - `EvictMarkOwner(iconID, deadGUID)` — [v0.26] GUID-aware eviction. Preserves state when `MarkMemory` already points to a new assignment.
  - `ReviewSkullState(callerID)` — **[v0.31]** now a thin **shell** over the pure `DecideSkullSuccessor` seam (architecture candidate A, PR #119; was ~190 lines of inline guards + decision). Flow: `ShouldDriveMarks()` permission gate → build the cheap skull-slot snapshot (live `mark8` liveness, `MemoryOwner(8)`, `NameFor`/`activeDB` sequential check — done once) → `DecideSkullSuccessor(snapshot, LiveBoard)` → dispatch the tagged intent (`adopt` → ledger-only `RegisterMarkUsage`, no re-`SetRaidTarget`; `assign` → `ApplyMarkIntent`, so succession flows through the one shared apply edge). The duplicate-event guard (`MemoryOwner[8]`), sequential guard, and the Governor/Incumbency rule (`GetBlockingMarkInfo()` + `FindEmergencyCandidate()` via `IncumbencyBlocks()`, roadmap #3) all moved **into** the pure decider and are now fixture-tested off-client. **[v0.29, swarm slice 3, PR #72]** the gate stays `ShouldDriveMarks()` — a record-before-apply path a non-queen must not reach — but now runs **shell-first** (the one accepted non-identical sliver: a non-marking drone logs `BLOCKED - not active` instead of a quiet confirm; debug-log-only). **[v0.28/v0.31, PR #55]** debug breadcrumbs move to a shell-side `LogSkullReview` (only ever called under `DebugEnabled`): the once-per-*distinct*-holder `confirm - mark8 alive (owned)` throttle (`lastAliveSkullLogged`) and the `registered pre-existing skull holder` adoption wedge tell are preserved. See **skull succession** in `CONTEXT.md`.
  - `VerifyMarkExistence()` — uses SuperWoW `mark` units for server-side tracking.
  - `ClearMarksForPullEnd()` — **[v0.28, PR #52]** at true pull-end, physically strips every **hostile-worn** mark (`mark1-8`, guarded by `not UnitIsPlayer` so manually-placed player marks survive) and calls `Ledger.Clear()`, so Turtle never retains a raid icon onto a respawn. **[v0.29, swarm slice 3, PR #72]** `ShouldDriveMarks`-gated (was `HasPermissions`) — it fires automatically on pull-end, so un-gated every drone would race to strip the queen's marks. Preserves the plan (`sessionAssignments` / profile DB / `disabledMarks` / sequential cursor / recorder). NOT `ResetSession`. See **Pull-End Mark Clear** under Key Systems.
  - `ResetSession()` — wipes all state including `MarkMemory`. **[v0.29, swarm slice 3, PR #72]** split-gated: the local-state reset is unconditional, but the physical `mark1-8` strip is `ShouldDriveMarks`-gated, so `/tmark reset` on a drone clears its own state without stripping the group's (queen's) world marks.
- **TankMark_Batch.lua**: Shift+mouseover batch marking system with delayed queue execution. **[v0.29, swarm slice 3, PR #72]** both guards (entry + mid-batch re-check) are `ShouldDriveMarks`-gated, so manual marking is queen-only; a drone gets a swarm-aware "X is the active marker" notice instead of placing marks. **[v0.30, Phase 4]** when `SmartMarkEnabled()`, `ExecuteBatchMarking` computes a whole-pack plan via `DecidePull` up front (sequential mobs partitioned out, icons reserved) and `ProcessBatchMark` applies it through the existing delayed queue + re-validation; toggle off = the classic per-mob path.
- **TankMark_Pull.lua**: **[v0.30, marking Phase 4]** the pre-fight **pack brain** — `DecidePull(candidates, board)`, pure + board-injected like `DecideMark`. Reasons over the whole Shift+mouseover pack at once and returns intents (applies nothing; the Batch shell feeds them to the delayed-apply queue). **KILL-FIRST + prio-ordered** (ADR 0002): the tank ladder (`getTankRoster`, `prio asc`) claims the lowest-prio mobs (skull to the kill-first), then CC claims the eligible **leftovers** taking the kill-LAST (`prio desc` / `seq desc`). Candidacy = `CCTierEligible AND (type=="CC" OR MeetsAutoCCFloor)`; `SelectCCSlot` routes each. **reserve-a-kill-target is automatic** — the ladder always kills the lowest-prio mob before CC, so a lone mob is killed and there is no demotion step. Sequential (`marks>1`) mobs are partitioned out and their icons reserved; IGNORE dropped; overflow + un-CCable-worthy surfaced (never silently truncated). Also `SmartMarkEnabled()` (`/tmark smartmark`, default off). Drives `tests/pull_assignment_spec.lua`. Loaded after `TankMark_Processor.lua`.
- **TankMark_Scanner.lua**: SuperWoW nameplate scanner with snapshot batching:
  - Initializes `TankMark.MarkMemory` table.
  - `StartSuperScanner()` — OnUpdate loop with 4 phases: Reset → Snapshot (capture nameplates, reinforce `MarkMemory` for known marks, buffer candidates) → Decision (priority-sort candidates, execute `ProcessUnit`) → Cleanup (`ReviewSkullState`). **[v0.28, PR #53]** the Cleanup call is gated: `ReviewSkullState("SCANNER_TICK")` runs only when `batchIndex > 0 or UnitExists("mark8")` (an unmarked in-combat candidate exists, or a skull token needs review); when neither holds it has nothing to do, so the idle tick skips it (and stops spamming the SKULL_REVIEW debug log). Death-driven skull reassignment is unaffected — it runs via the `COMBAT_LOG`/`UNIT_DEATH` callers in `Death.lua`, not the scanner tick. **[v0.28, PR #55]** for the complementary *in-combat* case (a skull stays up, so this gate keeps calling every tick), `ReviewSkullState`'s own steady-state confirm short-circuit keeps the per-tick SKULL_REVIEW noise down — see its entry above.
  - `IsGUIDInCombat()`, `IsNameplate()`, `ScanForRangeSpell()`, `Driver_IsDistanceValid()`.
- **TankMark_SyncCodec.lua**: **[v0.29, swarm slice 1, PR #66]** Pure, definition-only wire codec for addon-message sync — the single source of truth for the `M` mob-record format. `EncodeMob(zone, mob, data)` → wire string; `Decode(msg)` → typed record `{ kind="M", zone, mob, prio, mark, type, class }` or nil. Owns the field order, `;` separator, `M` tag, defaults (first-mark-only / `KILL` / `NIL`-class sentinel), and all validation (numeric prio/mark, 0–8 range). No WoW/Ledger/session state and no top-level execution, so the off-client harness loads it directly (unlike `Sync.lua`, which builds a frame at load). Loaded before `Sync.lua`. **[v0.29, swarm slice 4, PR #75]** now also owns two profile-sync types — `EncodeProfile`/`Decode`→`{kind="P"}` (the HUD-minimal `mark+tank+role` snapshot, role as 1-char `T`/`C`, one bad entry rejects the whole message) and `EncodePull`/`Decode`→`{kind="PR"}` — plus the `planVersion` field on the `Q` heartbeat. `Decode` now splits the tag on the first `;` (so the multi-char `PR` tag parses) instead of taking one char. **[v0.29, swarm slice 5a.1, PR #78]** adds the `H` type — `EncodeHandoff(target)` → wire string; `Decode` → `{ kind="H", target }` — the directed crown-pass offer (one field, the target name; empty rejected), structurally a sibling of the bare `PR`. **[v0.29, swarm slice 6, PRs #89/#91]** widens the `M` mark field to a `.`-joined **list** (`8.1.2`) so sequential marks transfer losslessly (a single mark is legacy-identical wire, so `Decode` returns `marks` array + `mark`=`marks[1]`), and adds the Mob DB *share* types — `SB`/`SE` (the broadcast-once frame begin/end: `SB;<poster>;<zone>;<count>` / `SE;<poster>;<zone>`) and `SR` (directed share-request `SR;<poster>;<zone>`) — plus the clickable chat-**link** grammar `EncodeShareLink`/`DecodeShareLink` (`tankmark:<poster>:<zone>`, a `|H..|h` hyperlink body, not an addon message). **[v0.29, swarm slice 7, PR #97]** adds the `HR` type — `EncodeHealerRecord(zone, version, mark, healers)` → `HR;<zone>;<version>;<mark>;<space-delim healers>` / `Decode` → `{kind="HR", zone, planVersion, mark, healers}` — the additive per-entry healer list layered onto a `P` (the healers `P` drops because a full profile with them overflows one ≤254B message); the trailing healer list is the greedy last field, mark validated 0–8 like `P`, and it cannot collide with the `H` handoff tag (Decode matches the whole tag). See **Sync Wire Codec**, **Manual Handoff**, and **Mob DB Sharing** under Key Systems.
- **TankMark_Trust.lua**: **[v0.29, swarm slice 6.1, PR #89]** the per-player **trust axis** for Mob DB sharing (`SWARM_DESIGN.md` §7.3) — one account-wide table `TankMarkDB.Trust[name] = "blocked"|"trusted"` (absent = neutral), precedence Blocked > Trusted > Neutral. Pure + definition-only (no WoW API), so the off-client harness loads it: `Resolve(stored)` is the pure normalizer; thin stateful accessors `StateOf`/`IsBlocked`/`IsTrusted`/`Set`/`Clear` bind it to the table (lazily seeded, also in `InitializeDB`). Loaded after `SyncCodec`, before `Sync`. Consumed by the Options trust list (6.2) and the share-plane gates in `Sync.lua` (6.4). See **Mob DB Sharing**.
- **TankMark_Sync.lua**: Raid data synchronization via `TM_SYNC` addon messages. **[v0.29, swarm slice 0, PR #64]** the TWA/BigWigs inbound integration (`HandleTWABW`, `TWA_MarkMap`) is **removed** — one TM-native dialect now. `HandleSync` decodes via `SyncCodec.Decode` then performs the `TankMarkDB.Zones` write — the lone stateful apply edge (mirrors Ledger / `ApplyMarkIntent`); `BroadcastZone` encodes via `SyncCodec.EncodeMob`. Trust-gated by `IsTrustedSender` (raid Assist/Leader or party leader). **[v0.29, swarm slice 2, PR #69]** a decoded `Q` heartbeat routes to `Swarm.OnHeartbeat` (same trust gate); `TankMark.SyncPrefix` is exposed so the swarm beats on the one transport. **[v0.29, swarm slice 4, PR #75]** a decoded `P` routes to `Swarm.OnProfile` and a `PR` to `Swarm.OnPullRequest` — both behind the same rank≥1 `IsTrustedSender` gate, and `OnProfile` *additionally* requires `sender == Swarm.currentQueen` before it overwrites the local plan (so a rank≥1 non-queen can pollute comms but never rewrite a drone's HUD). **[v0.29, swarm slice 5a.3, PR #80]** a decoded `H` (handoff offer) routes to `Swarm.OnHandoffOffer` behind the same `IsTrustedSender` gate; `OnHandoffOffer` then requires `sender == Swarm.currentQueen` (mirrors `OnProfile`), `target == self`, and local eligibility before it acts, and performs **no DB write** (the offer only nudges the election's claimant set). The profile is queen-authoritative and deliberately carved out of consent (§6.1). **[v0.29, swarm slice 6, PRs #91/#92/#93]** the Mob DB now has its full **advertise → pull → consent** sharing here (`SWARM_DESIGN.md` §7), and the old unsolicited push is **gone**: `BroadcastZone` is removed and the legacy naked-`M` auto-apply is dropped — an `M` applies **only** inside a consent-gated share frame. `HandleSync` decodes first (pure) then splits a **share plane** (consent-only, NO rank gate, block-checked) — `SR`→`OnShareRequest`, `SB`/`SE`/framed-`M`→the receiver buffer — from the rank-gated **control plane** (`Q`/`P`/`PR`/`H`). Poster side (6.3): `PostShareLink(zone)` posts the link; `OnShareRequest` coalesces a click-storm (4s) under a per-zone cooldown (10s) into one `BroadcastShareFrame`. Receiver side (6.4a): `OnShareLinkClick` fires the pull + arms a pending-click (15s); `OnShareBegin`/`OnShareRecord`/`OnShareEnd` buffer **solicited-only** keyed on the unspoofable `sender` (§7.5), validate the count all-or-nothing, then the trust gate → `ApplyShare` (snapshot + full-zone replace). **[v0.29, late-join fix, PR #98]** the `PR` pull-request is now routed **before** the control-plane rank gate (a drone is a passive renderer and is typically rank-0, so the old placement dropped its pull and stranded late joiners until the queen's next Save) — safe because `OnPullRequest` only sets a coalesced pending-push *when we are the queen*, rebroadcasting already-group-public data; mirrors the slice-6 share plane's pre-rank-gate data requests. **[v0.29, swarm slice 7, PR #100]** a decoded `HR` (healer record) routes to `Swarm.OnHealerRecord` on the rank-gated control plane (same trust as `P`: rank≥1 gate + `OnHealerRecord` requires `sender == currentQueen`). See **Mob DB Sharing** under Key Systems.
- **TankMark_Swarm.lua**: **[v0.29, swarm slice 2, PR #69]** Queen/drone control plane that computes & displays who the marking queen is. **[v0.29, swarm slice 3, PR #72]** it is no longer display-only: `Permissions.ShouldDriveMarks()` now reads `Swarm.selfAmQueen` (the stored election output) and `Swarm.IsRunning()` (the `Swarm.frame ~= nil` liveness predicate) to gate every marking path — so a non-queen yields. The election core itself is unchanged and still touches no marking path. Mirrors the SyncCodec/Sync split: a **pure election core** (`DeterministicMax`, `ElectQueen` — the claimant-count rule unifying election / stickiness / split-brain, `ComputePresence` — two-filter present set, `DeriveRole`) with no WoW state / no frame / no top-level execution, so the off-client harness loads it directly; plus a **runtime shell** (`InitSwarm` deferred + SuperWoW-gated builds the 5s beat frame, `Recompute` orchestrates roster build → bootstrap entry/exit → election → live HUD repaint, `UpdateNotice` is the ≥1-cycle debounced chat announcer, `OnHeartbeat`/`OnRosterChange`/`Tick` are the triggers). Loaded after `Sync.lua`. **[v0.29, swarm slice 4, PR #75]** it also carries the profile-sync data plane: the queen-side `OnProfileSaved`/`OnPromoted` (bump the global `planVersion` + `PushProfile` the zone snapshot) and `OnPullRequest`/`FlushSync` (coalesced PR-response, one broadcast per zone per tick), the drone-side `OnProfile` (queen-gated apply, empty-keeps), `EvaluatePull` (the `(queen,version,zone)`-triple refetch predicate), and `SendPull`; runtime state `planVersion`/`appliedKey`/`needPull`/`versionHeard`/`pendingPush`. Loaded after `Sync.lua`. **[v0.29, swarm slice 5a, PRs #79/#80]** it also owns the manual-handoff protocol: the pure `AdvertisedClaim(selfAmQueen, pendingClaim, relinquish)` (the claim bit fed to `ComputePresence`/`SendBeat`, split from the marking-gate `selfAmQueen` so a handoff moves the crown through the election, never by writing `selfAmQueen`), the queen-side `InitiateHandoff` (validates queen-only + live-candidate target, broadcasts `H`, arms the offer TTL) and `ExpireHandoffState` (the two TTLs), the receiver `OnHandoffOffer` (4 gates + auto-accept → `pendingClaim` + forced beat), and the `Recompute` integration (hear-the-claim → `relinquish` one-shot + forced beat; rising-edge `pendingClaim` clear); runtime state `pendingClaim`/`pendingClaimUntil`/`relinquish`/`pendingHandoffTarget`/`handoffOfferUntil`. **[v0.29, swarm slice 5b, PRs #84/#86]** adds `HandoffCandidates()` (the single-sourced present-minus-self set, used by the handoff menu, the status-line cue gate, and the recorder prompt's notice) and the thin `SelfIsSoleCandidate()`; `Recompute` now also repaints the HUD on a `lastHandoffAvail` flip (the handoff cue depends on the candidate **set**, which changes without a role/queen transition) and fires `PromptRecorderOnPromotion` on the `OnPromoted` rising edge when recording. **[v0.29, swarm slice 7, PRs #99/#100]** it carries healer-assignment sync: `PushProfile` appends one `HR` per healer-bearing entry after the `P` (queen side, #99), and the drone-side `OnHealerRecord` layers an `HR` onto the entry the matching `P` already applied — queen-only + `planVersion`-gated, sets `.healers`, re-renders via `ApplyProfileToSession`/`RefreshProfileTabForZone`; a stale/missing-entry `HR` is a no-op and it never touches `appliedKey` (Cut 1: a lost `HR` self-heals on the next push). **[v0.29, late-join reliability fixes, PRs #95/#100]** four hardening fixes, all rooted in 1.12 `IsPartyLeader()` being unreliable in BOTH directions (it flickers **false** for the real leader around combat-end *and* **true** for a non-leader on a join's `PARTY_MEMBERS_CHANGED` — every candidacy/heartbeat signal must debounce both): `CANDIDATE_GRACE` (5s) debounces a candidacy *loss* in `Recompute` so a combat-end false-flicker no longer deposes the queen and re-elects every kill (#95); `SendBeat` gates on the *debounced* `wasCandidate` (not the raw `SelfIsCandidate()`) so a queen held through a blip keeps heartbeating — else drones time out its presence and churn `DRONE↔NONE` (#100); bootstrap entry is **skipped when already following an external queen** so a candidacy-*gain* flicker doesn't tear a settled drone into a 15s listen (#100); `OnProfileSaved` clears `appliedKey` for the saved zone so a local reset/edit re-pulls instead of `keyMatches` reporting "synced" over an emptied profile — *the* "no sync on join" cause in a reset-without-`/reload` loop (#100); and the pull/render path reads the healed `GetCachedZone` via a `currentZone` helper, not the raw `GetRealZoneText()` that returns `""` in the cold-login window (#100, defensive). See **Swarm Control Plane**, **Profile Sync**, **Manual Handoff**, and **Promotion UX** under Key Systems.

### Data/
Database management and persistence

- **TankMark_Data.lua**: DB initialization (`InitializeDB`), corruption detection (`ValidateDB`), snapshot system (`CreateSnapshot`/`RestoreFromSnapshot`), lazy-load zone data (`LoadZoneData` — **[v0.31]** now a thin shell over `ZoneView.Merge`, which builds the validated **active zone view** — and `RefreshActiveDB`), roster management (`UpdateRoster`/`GetFirstAvailableBackup`), and the **Debug Logging System**:
  - `DebugLog(category, message, data)` — circular buffer (500 entries max), stored in `TankMarkDB.DebugLog`. Early-returns if `TankMark.DebugEnabled == false`, then **[v0.28, PR #49]** drops any category not in the optional `TankMark.DebugCategories` capture-time allow-list (`nil` = log every category).
  - `DumpDebugLog(filterCategory)` — prints to chat with color-coded categories.
  - `ClearDebugLog()` — wipes the buffer.
  - `UpdateZoneDropdowns()` — syncs Mob DB and Profile dropdowns on zone change.
- **TankMark_Defaults.lua**: Default mob database (shipped data).

### UI/
All visual components

- **TankMark_HUD.lua**: In-game heads-up display (8-row mark tracker). **[v0.29, swarm slice 2, PR #69]** `RenderSwarmLine()` paints a bottom-anchored swarm status line (queen name + derived role) in **both** render paths (profiled and `NO PROFILE LOADED`); display-only, reads live `Swarm` state, reserves 16px when shown. **[v0.29, swarm slice 5b, PRs #84/#86]** also hosts the promotion UX: the recorder-on-promotion `StaticPopup` (`PromptRecorderOnPromotion`/`WarnRecordingQueen`) and the handoff trigger — `RenderSwarmLine` toggles a transparent overlay `Button` + a down-arrow `Texture` cue over the status line for a queen with candidates; `InitHandoffMenu`/`ConfirmHandoff` route a pick through `Swarm.InitiateHandoff`. **[v0.29, swarm slice 6.4a, PR #92]** also hosts the Mob DB **share receiver UI** (Core has no UI): the global `SetItemRef` hook (pfQuest pattern — only `tankmark:` links are intercepted → `OnShareLinkClick`, everything else falls through to the original) and `ShowShareConfirm` — the Neutral-sender import dialog as a small **custom 3-button frame** (Import / Always trust / Cancel), because 1.12 `StaticPopup` is only 2-button and a custom frame also sidesteps the Turtle Escape-skips-OnCancel quirk. See **Promotion UX** and **Mob DB Sharing** under Key Systems.
- **TankMark_Options.lua**: Config panel entry point and tab management. **[v0.29, swarm slice 6.2, PR #90]** the near-empty **Options** tab (tab 4, `BuildGeneralOptionsTab`) now hosts the Mob DB sharing **trust list** via private section builders (`CreateMarkNormalsSection` / `CreateTrustSection`, per the UI convention): one backing table (`TankMarkDB.Trust`) rendered as a `FauxScrollFrame` allow/block list (trusted green above blocked red), an add-by-name EditBox + Trust/Block buttons, and per-row state-toggle + remove. `RefreshTrustList` repaints on tab open (`SwitchTab` tab 4) and after every edit. **[v0.31]** the same tab now also surfaces the three marking toggles as persisted checkboxes — `CreateMarkNormalsSection` (Mark Normals, now `TankMarkCharConfig.markNormals`-backed) and `CreateMarkingAutomationSection` (**Smart Pre-Marking** / **Auto-CC in Combat**, each with a `GameFontDisableSmall` sub-legend) — with a tab `OnShow` that re-syncs all three from their accessors (the panel is built once + cached, so a slash toggle made while it was closed would otherwise leave the box stale on reopen) and also repaints the trust list; the legacy "Database Resilience System" footer was removed. See **Mob DB Sharing**.
- **TankMark_UI_Widgets.lua**: Reusable UI components (dropdowns, buttons, etc.)
- **Config/**: Config panel tabs, split into two subdirectories:

#### Config/Database/ — Mob Database tab
- `TankMark_Config_Mobs.lua` — State registry: `mobRows`, `selectedIcon`, accordion state flags (`isAddMobExpanded`, `isSequentialExpanded`, `isSequentialActive`), and all Mob tab widget references. **[v0.30, marking Phase 3]** also `selectedRole` (the authored mob role) and `editTier` (the tier of the mob currently in the editor — the non-stale source `ApplyRoleDefaults` reads, set on Target-detect and edit-open, cleared on reset).
- `TankMark_Config_Mobs_UI.lua` — [v0.27] **UI construction only.** All layout is expressed as private `local function` section builders (`CreateZoneControls`, `CreateMobList`, `CreateSearchBox`, etc.) called from the `CreateMobTab(parent)` entry point. Container frame uses `TOPLEFT 0,0 / BOTTOMRIGHT 0,0`. Zone dropdown anchored at `TOPLEFT 44,-43`. List background left edge at x=31 from the window. Exposes `UpdateMobZoneUI()`. **[v0.30, marking Phase 3]** the editor adds the **Role** button (`TankMark.roleBtn`, on the CC-Class row at `x=162,-43`, width 64 to stay clear of the Class button) wired to `InitRoleMenu`.
- `TankMark_Config_Mobs_List.lua` — Mob list rendering and `UpdateMobList()` scroll logic. **[v0.29, swarm slice 6.3, PR #91]** in Manage Zones (zone) mode each row shows a **Share** button (`row.share`, left of Delete) → `TankMark:PostShareLink(data.label)`, so any zone's Mob DB can be shared (not just the current one). **[v0.30, marking Phase 3]** the edit-row OnClick also populates `selectedRole`/`editTier` from the row data (mirroring `selectedClass`) so the Role dropdown round-trips an untouched edit; `role` is **no longer echoed** in the read-only metadata line (the dropdown is its sole control — the line now shows `creatureType / tier` only).
- `TankMark_Config_Mobs_Logic.lua` — Save/load/delete mob data, smart defaults, `ResetEditorState()`. **[v0.30, marking Phase 3]** `ApplyRoleDefaults(role)` — the thin UI wrapper over Core `RoleTierPrio(role, editTier)` that pre-fills the prio field (the human always overrides), `UpdateRoleButton()`, and the `SaveFormData` role write: `mobEntry.role = TankMark.selectedRole`, **split from** the Phase-1 `creatureType`/`tier` preserve block so `role` is written as an editable field (populated from `existing.role` on edit-open) rather than blindly carried forward.
- `TankMark_Config_Mobs_Sequential.lua` — Sequential mark configuration: `RefreshSequentialRows()`, `OnAddMoreMarksClicked()`, `RemoveSequentialRow()`, `ActivateSequentialAccordion()`.
- `TankMark_Config_Mobs_Menus.lua` — Context menus: `InitIconMenu()`, `InitClassMenu()`, `InitSequentialIconMenu()`, `InitSequentialClassMenu()`. **[v0.30, marking Phase 3]** `InitRoleMenu()` — the mob-Role dropdown (No Role / HEALER / CASTER / MELEE); picking a real role calls `ApplyRoleDefaults`, "No Role" just clears it (no re-derive).

#### Config/Profiles/ — Team Profiles tab
- `TankMark_Config_Profiles.lua` — State registry and data logic: `profileRows`, `profileCache`, `profileZoneDropdown`, `profileScroll`, profile templates (`TankMarkProfileTemplates`), `LoadProfileToCache()`, `SaveProfileCache()`, **[v0.29, swarm slice 4, PR #75]** `ApplyProfileToSession(zone)` (the shared seam factored out of `SaveProfileCache` — rebuild `sessionAssignments` + `UpdateHUD` only, no Print/dropdown-read/list-refresh/marking — used by both the queen's Save and the drone's `Swarm.OnProfile` apply edge; `SaveProfileCache` now also calls `Swarm.OnProfileSaved` so a queen push-on-Saves), `UpdateProfileList()`, `ProfileAddRow()`, `ProfileDeleteRow()`, `ToggleProfileZoneBrowser()`, `InferRoleFromClass()`, `InitProfileIconMenu()`, `AddHealerToRow()`, `UpdateProfileZoneUI()`, `ShowTemplateMenu()`, `ShowCopyProfileDialog()`, `RequestResetProfile()`. **[v0.29, swarm slice 5b, PR #83]** the drone read-only gate: `ApplyProfileEditGate()` (disable writes / show banner when `Swarm.IsRunning() and Swarm.lastRole=="DRONE"`, called at the tail of both `UpdateProfileList` modes + tab `OnShow`), `RefreshProfileGateIfVisible()` (called from the `Recompute` role-transition seam, visibility-guarded), and `RefreshProfileTabForZone()` (live-refresh the open tab on a queen's `Swarm.OnProfile` push). See **Promotion UX** under Key Systems.
- `TankMark_Config_Profiles_UI.lua` — [v0.27] **UI construction only.** Layout is expressed as five private `local function` section builders, each receiving `parent` (the tab container frame) and returning nothing except `CreateListArea` which returns `psf` for internal use:
  - `CreateTopRow(parent)` — Zone dropdown (`TMProfileZoneDropDown`, `TOPLEFT 44,-43`, matching Mobs tab), Manage Profiles checkbox (`TMManageProfilesCheck`, `TOPLEFT 243,-45`), Save Profile button (`TMProfileSaveBtn`, `TOPLEFT 372,-45`).
  - `CreateColumnHeaders(parent)` — Four `GameFontNormalSmall` labels at y=-85.
  - `CreateListArea(parent)` — `FauxScrollFrame` `TankMarkProfileScroll` at `TOPLEFT 37,-100`; backdrop `plistBg` anchored to the scroll frame. Left edge aligns with Mobs tab list background (both at 31px from the window).
  - `CreateRowPool(parent)` — Pool of 8 reusable `Frame` rows at x=40, y=-100-(i-1)*30. Each row contains: `row.zoneLabel` (hidden FontString for zone browser mode), icon button, tank edit box + T button, healer edit box + T button, offline-healer warning icon with tooltip, CC checkbox, Delete button. All references stored in `TankMark.profileRows[i]`.
  - `CreateBottomBar(parent)` — Add Mark / Use Template / Copy From / Reset buttons.
  - `CreateProfileTab(parent)` — Entry point. Container `t2` uses `TOPLEFT 0,0 / BOTTOMRIGHT 0,0` (same coordinate origin as Mobs tab `t1`). **[v0.29, PR #83]** also creates the read-only banner (`profileReadOnlyBanner`), stores `TankMark.profileTabFrame`, and wires `t2:OnShow` → `ApplyProfileEditGate` (the drone gate). `CreateRowPool` stores `row.iconBtn` for the gate.

#### Config/TankMark_Config_Data.lua — Data Management tab
Standalone file (not in a subdirectory). Snapshot restore, default merging, export/import UI. `BuildDataManagementTab(parent)`.

### Layout Alignment Convention (v0.27)
Both tab container frames (`t1` for Mobs, `t2` for Profiles) use `TOPLEFT 0,0 / BOTTOMRIGHT 0,0`. All pixel offsets inside both tabs are therefore in the same coordinate space. Key shared landmarks:

| Element | x offset from window | Anchor in code |
|---|---|---|
| Zone dropdown | 44px | `TOPLEFT 44, -43` |
| Manage checkbox | 243px | `TOPLEFT 243, -45` |
| List/scroll bg left edge | 31px | Mobs: `listBg TOPLEFT 31,-79`; Profiles: `psf TOPLEFT 37,-100` (bg inset adds 6px) |

Maintaining these shared anchors ensures no element "jumps" when switching tabs.

### Other
- **Bindings.xml**: Key binding definitions.
- **TankMark.toc**: Addon manifest and load order.

## Key Systems (v0.26)

### MarkMemory
A table (`TankMark.MarkMemory`) mapping `iconID → GUID` that tracks which mob currently holds each raid mark. Initialized in `TankMark_Scanner.lua`. Reinforced every scanner tick for visible marks. Flushed on combat end (`PLAYER_REGEN_ENABLED`, **partial** — `MarkMemory` only, so marks survive the addon-holder's own death) and session reset. **[v0.28, PR #52]** at pull-end *when alive*, `ClearMarksForPullEnd()` additionally does a full `Ledger.Clear()` (all four indices) plus a physical `mark1-8` strip — see **Pull-End Mark Clear**.

**Used by:** `IsMarkBusy()`, `GetMarkOwnerPriority()`, `EvictMarkOwner()`, `ReviewSkullState()`, `ProcessUnit()` (stale GUID cleanup), `GetBlockingMarkInfo()`, `GetFreeTankIcon()`.

### Decision Layer (decide / apply split + ports board)
**[v0.28, roadmap #2]** `ProcessUnit()` no longer decides *and* marks in one pass — the two concerns are split so the decision is inspectable and testable in isolation.

**Tier 1 (decide / apply, PR #41).** `DecideMark()` is the single decision seam: it returns an **intent** (`{ icon, reason, wasBusy?, override? }`) and applies **nothing**. `ProcessUnit()` then hands that intent to `ApplyMarkIntent()` — the **sole** decide-path apply edge (`RegisterMarkUsage` Ledger record → `Driver_ApplyMark`). So every automatic mark flows decide → intent → apply, and a decision can be exercised without touching the game. **[v0.31]** the **death-path** skull decision joins the seam too (candidate A, PR #119): `DecideSkullSuccessor` returns a tagged `{ action }` intent that the `ReviewSkullState` shell dispatches (`assign` through the same `ApplyMarkIntent` edge), so both automatic marking brains are now pure and fixture-tested.

**Tier 2 (ports board, PR #58).** The decide functions are **dependency-injected**: their only window onto the world is a `board` parameter — a table of closures over the live `TankMark` methods (state **reads**: combat/busy/priority/free-icon/blocker/CC/disabled) plus one side-effect **sink** (`logDecision`). Production passes `TankMark.LiveBoard` (`Core/TankMark_Processor.lua`); the off-client suite passes a mock. The closures resolve at **call time**, not build time, so within a single scanner tick a `board.getFreeTankIcon()` for candidate #2 already sees candidate #1's just-recorded `Assign` — a frozen snapshot would not. The board carries only state reads and the one sink; **pure** predicates such as `IncumbencyBlocks()` are not ports (they read no state) and stay direct calls. The result is a `DecideMark` that is **zero-global** apart from the pure-language `L._tgetn`, which is what lets the decision tree run under `lua5.1` with no WoW client — see **Testing**.

### Governor Check (Skull Incumbency)
Prevents skull from being assigned to a mob when existing marked mobs have equal or higher priority. **[v0.28]** The decide-path gate is `GovernorBlocks()` (shared by `DecideKnownMark`/`DecideUnknownMark` via the `allowSteal` flag); the death-path gate is the pure `DecideSkullSuccessor()` (**[v0.31]** — via the `ReviewSkullState` shell; both now live in `Core/TankMark_Processor.lua`). **Both share one incumbency comparison** — `IncumbencyBlocks(myPrio, blockIcon, blockPrio)` in `TankMark_Assignment.lua` (roadmap #3, PR #43) — so the `>=` operator is single-sourced and can't drift. The PR #43 consolidation deliberately left the *policy* questions frozen; **[v0.28, PR #47]** the equal-prio **sheep-edge is now fixed** — a parked/CC'd incumbent is excluded from blocking via `IsMarkCCd()` (in `GetBlockingMarkInfo`/`UpdateBest`), while the `>=` operator itself is unchanged (a *non*-CC'd equal-prio incumbent still blocks). **[v0.30, Phase 4]** the same `UpdateBest` chokepoint additionally excludes a mob on a **CC-role slot** via `IsCCSlotMark()` — even before its Polymorph/Banish aura lands — so an auto-CC'd or authored-CC mob never suppresses the pack's skull (the bug the CC-model revision fixed). **[v0.28]** the unknown-path `allowSteal` asymmetry is now **resolved** (roadmap #3's last loose end): it is a *deliberate* split, not a deferred question — `known=true` honors the mob's DB designation (asserts the plan over a phantom/foreign holder), while `unknown=false` has nothing to assert and stays hands-off an occupied skull. No behavior change: unknown never stealing was already the shipped Tier-1 state.

### Pull-End Mark Clear
**[v0.28, PR #52]** Turtle WoW retains raid-target icons on mobs through death AND respawn, so a respawn wearing a retained skull could be adopted by `ReviewSkullState` into `MarkMemory[8]` and wedge skull reassignment for the rest of the run. `ClearMarksForPullEnd()` (`Core/TankMark_Death.lua`) fixes this at the source: on `PLAYER_REGEN_ENABLED`, when the addon-holder is **alive** (`not UnitIsDeadOrGhost("player")` — a tank's only alive combat-exit is the pull resolving), it physically strips every **hostile-worn** mark (`mark1-8`, guarded by `not UnitIsPlayer` so manually-placed player marks survive) and calls `Ledger.Clear()`. The raid leader's plan survives (`sessionAssignments`, profile DB, `disabledMarks`, sequential cursor, recorder are not Ledger indices) — this is **not** `ResetSession`. The dead/ghost path is deliberately skipped so marks survive the addon-holder's own death (same rationale as `FlushMemory`'s partial wipe). **Cut 1 ships the primary trigger only**; a scanner-tick fallback for the die→raid-finishes-pull→late-rez gap (no post-rez `REGEN`) and `RAW_COMBATLOG` engaged-set tracking are deferred to phase 2.

### Debug Logging
Toggle with `/tmark debug on|off`. Dump with `/tmark debug dump`, optionally filtered by category: `/tmark debug dump APPLY`, `/tmark debug dump DECIDE`, `/tmark debug dump BUSY` (any category, case-insensitive). Clear with `/tmark debug clear`. **[v0.28, PR #49]** Filter at *capture time* with `/tmark debug only <cats...>` (e.g. `/tmark debug only PROCESS DECIDE APPLY` — the ring then stores ONLY those categories, so a spammy category like `SKULL_REVIEW` ~2-4 entries/tick can't evict the decision entries before you read them); `/tmark debug all` clears the filter; `/tmark debug only` with no args prints it. Runtime-only (resets on `/reload`, like `DebugEnabled`). **[v0.28]** `/tmark debug ccscan` dumps the current target's debuffs with their aura IDs (the 4th `UnitDebuff` return) for gathering parked-CC IDs into `CCAuraSet` — a dev helper retained until the CC list is validated against Turtle.

**Performance rule:** All `DebugLog()` call sites MUST be wrapped in `if TankMark.DebugEnabled then ... end` to prevent argument construction when debug is off. See `Core/TankMark_Permissions.lua` `Driver_ApplyMark()` for the canonical pattern.

### Ownership Verification
In `ProcessUnit()`, when SuperWoW is available, `GetRaidTargetIndex(guid)` results are cross-checked against `UnitExists("mark"..icon)` to detect stale marks from theft scenarios.

### Sync Wire Codec
**[v0.29, swarm slice 1, PR #66]** The `M` mob-record wire format is single-sourced in the pure `Core/TankMark_SyncCodec.lua` (`EncodeMob` / `Decode`), replacing two hand-rolled copies that previously lived apart in `Sync.lua` — the encoder in `BroadcastZone`, the decoder in `HandleSync` (the original roadmap #4 defect). Same shape as the Decision Layer: a **pure** core (decode → validate → reject malformed → typed record; no WoW/Ledger/session state) plus a **single guarded apply edge** — `HandleSync`'s `TankMarkDB.Zones` write — that owns the stateful effect. This is what makes the codec unit-testable off-client (see **Testing**), and it is the intended substrate for the swarm's wider message set (heartbeat, profile, handoff — `SWARM_DESIGN.md` §8). **[v0.29, swarm slice 0, PR #64]** the second dialect (TWA/BigWigs inbound) is removed, so the codec is purely TM-native. **[v0.29, swarm slice 6]** the codec grew the Mob DB share types (`SB`/`SE`/`SR`) and the chat-link grammar, and the `M` mark field widened to a `.`-joined list; the receiver-side consent it once deferred is now built — see **Mob DB Sharing**.

### Zone Cache & Cold-Login HUD Refresh
**[v0.29, PR #65]** `TankMark.currentZone` caches the current zone; `GetCachedZone()` is the read accessor. On a *cold* login `GetRealZoneText()` can return `""` before the zone APIs are ready, and `""` is truthy in Lua — so a naive cache sticks on the empty string and every `TankMarkProfileDB[zone]` lookup (and the HUD) misses, showing **"NO PROFILE LOADED"**. Fixed three ways: `GetCachedZone()` treats `""` like nil; a new **`PLAYER_ENTERING_WORLD`** handler (the first reliable zone moment on login, and it fires after every loading screen) re-reads the zone, runs `LoadZoneData`, and repaints the HUD when grouped; and `ZONE_CHANGED_NEW_AREA` now repaints too. Vanilla gotcha to remember: zone APIs are unreliable until `PLAYER_ENTERING_WORLD` / `ZONE_CHANGED_NEW_AREA` — `PLAYER_LOGIN` is too early.

**[v0.29, PR #85]** A *second* staleness source, exposed by the swarm: leaving all groups used to stamp a fake `currentZone = "SOLO_RESET"` as a once-only `ResetSession` guard — and it was **never cleared on rejoin**. Since `GetCachedZone()` only self-heals `nil`/`""` (not the sentinel), the fake zone stuck, so every `TankMarkProfileDB[zone]` lookup missed — breaking **both** the HUD (`NO PROFILE LOADED` despite a populated profile) **and** `CanAutomate()` (→ `SelfIsCandidate` false → a rejoined Assistant silently dropped from the queen election). The Profiles tab read a different zone source (`GetRealZoneText()`), so it still showed the real profile — the diagnostic contradiction. Fixed by retiring the fake-zone hack for a dedicated `TankMark.soloState` boolean (cleared on rejoin), so `currentZone` always holds a real zone.

### Swarm Control Plane
**[v0.29, swarm slice 2, PR #69]** `Core/TankMark_Swarm.lua` is the queen/drone consensus — **display-only** at this slice (it computes & shows who the single authorized marker *would* be; it changes **no** marking behavior, and only ever *reads* `CanAutomate`). Same pure-core + thin-shell shape as the Decision Layer and the Sync Wire Codec: the **election is a set of pure functions** the shell feeds plain tables, so it is unit-tested off-client (see **Testing**) before it ever touches a real raid.
- **Candidacy = `CanAutomate()`** (the existing automation gate, read-only). The two-filter **present set** (`ComputePresence`): *self* is present iff it is a candidate (from the gate, not heartbeats — we don't hear our own beats); any *other* is present iff heard within the 15s window **and** currently holds roster rank ≥ 1, so a **demote** drops it instantly (eligibility filter via `RAID_ROSTER_UPDATE`) while an **unclean DC** drops it at the window timeout (presence filter).
- **Election is deterministic** (`ElectQueen`), driven by the **claimant-count rule** that unifies three behaviors: **≥2** self-declared claimants → split-brain tiebreak `DeterministicMax(claimants)` (highest roster rank, then lowest name — *overrides* stickiness); **1** claimant → stickiness (the lone incumbent is kept, even against a higher-rank non-claimant that appeared later); **0** → fresh election over all present candidates (bootstrap-resolve or failover). Rank is read from the roster, never trusted from a payload.
- **Role is derived, not stored** (`DeriveRole` → `QUEEN`/`DRONE`/`NONE`/`BOOTSTRAP`). A fresh candidate opens a **15s bootstrap listen-window** (= `INTERVAL*MISS`), beating `amQueen=false` and deferring to any heard incumbent, before it may assert the crown.
- **Transport:** candidate-only `Q` heartbeat (5s interval / 3-miss = 15s) on the shared `TM_SYNC` prefix + 0.3s throttle; the wire carries `amQueen` plus **[v0.29, slice 4]** `planVersion` (the queen's profile counter, 0 for a non-queen — see **Profile Sync**) (`SWARM_DESIGN.md` §8). `HandleSync` routes `Q` to `Swarm.OnHeartbeat` behind the same `IsTrustedSender` gate as `M`.
- **Display:** the HUD bottom line (`RenderSwarmLine`) follows the election live; the chat notice (`UpdateNotice`) is **debounced ≥ 1 heartbeat cycle**, so the ~1s `NONE` flicker on a leader handover never reaches chat (BOOTSTRAP and solo commit silently). The `SWARM` debug category (`/tmark debug dump SWARM`) is the acceptance instrument.

### Single-Marker Enforcement
**[v0.29, swarm slice 3, PR #72]** The one slice that makes a non-queen actually *yield* — acting on the election above. It splits the two concepts slice 2 had conflated (`Swarm.SelfIsCandidate()` reads `CanAutomate()`):
- **`CanAutomate()` — UNCHANGED, the *candidacy* gate.** `SelfIsCandidate` keeps reading it, so the election candidate set and **failover pool are preserved**. Folding the queen-check into `CanAutomate` would have dropped every non-queen out of the candidate set and broken failover — the trap this split avoids.
- **`ShouldDriveMarks()` — the *marking* gate** (`Core/TankMark_Permissions.lua`): `CanAutomate() and (not Swarm.IsRunning() or Swarm.selfAmQueen)`. **Fail-open** — if the election shell isn't running, degrade to today's eligible-clients-mark rather than going silent (the server rank-gate is the real backstop). No circular dependency: it reads the **stored** `selfAmQueen` field, never `CanAutomate`'s queen-status.
- **Every world-mark `SetRaidTarget` path reads it** — not just the `CanAutomate` sites. Ten gates migrated: the scanner top (a drone does **zero** scan work — no snapshot/Reaffirm/decision; recorder bypass preserved), `HandleCombatLog`/`HandleDeath`/`UnmarkUnit`, both Batch guards, the `Driver_ApplyMark` backstop (the sole edge = authoritative enforcement point), **and three former `HasPermissions` paths** the audit surfaced: `ClearMarksForPullEnd` (automatic — un-gated every drone raced to strip the queen's marks at pull-end), the `ResetSession` physical strip (split from its local-state reset), and `ReviewSkullState` (a record-before-apply path). Untouched: `BroadcastZone` (sync eligibility), `SelfIsCandidate` (candidacy), the load-time NUCLEAR wipe (pre-election, self-healing), HUD texture calls.

In-game 2-box verified (queen marks / drone silent / drone DRONE-deference / failover reclaim with no gap). `SWARM_DESIGN.md` §5.9 + §12 row 3.

### Profile Sync
**[v0.29, swarm slice 4, PR #75]** The drone-visibility payoff: the queen pushes its **Team Profile** so drones render the plan, filling the 8-row HUD mark grid slice 3 left blank for non-queens. **Display == enactment** — a drone overwrites its own `TankMarkProfileDB[zone]` slot, so a promoted ex-drone marks off *exactly* the plan it was showing. `SWARM_DESIGN.md` §6.1.
- **Storage — single-slot overwrite, queen-authoritative.** `Swarm.OnProfile` overwrites `TankMarkProfileDB[<zone>]` directly (no separate drone cache, no backup) and re-renders via the shared `ApplyProfileToSession` seam. The profile is **carved out of §7's consent rule** (the queen is an elected, rank-gated authority you already delegated marking to; team profiles are operational and rebuilt per run) — unlike the curated Mob DB, which keeps offer/accept. Healers were **omitted from the `P` wire** (a full profile with them overflows one ≤254B message) — **[v0.29, swarm slice 7, PRs #97/#99/#100]** they now travel as additive per-entry **`HR`** records layered on the *untouched* `P`: the queen appends one `HR` per healer-bearing entry after the `P`, and the drone's `OnHealerRecord` applies it queen-only + `planVersion`-gated (sets `.healers`, re-renders), so death-alerts, the Profiles tab, and a promoted ex-drone inherit healer assignments. Best-effort (Cut 1: re-sent each push, self-heals on push/pull; an `HR` lost while the version is stable persists until the next bump — accepted). The `HR` apply never touches `appliedKey`, so it can't strand the profile pull.
- **Versioning — global runtime `planVersion`.** A single monotonic counter, bumped on every queen-side Save (`OnProfileSaved`) and on promotion (`OnPromoted`), advertised on the `Q` heartbeat. Each drone records the `(queenName, planVersion, zone)` triple it last applied (`appliedKey`).
- **Refresh — three triggers.** (1) **Push-on-Save** — `SaveProfileCache` → `OnProfileSaved` bumps + `PushProfile` (the fast path). (2) **Push-on-promotion** — `Recompute` detects the `selfAmQueen` rising edge → `OnPromoted` bumps + pushes the current zone, so a handoff converges even when the new queen's plan changed without a Save (e.g. a pre-promotion drone-side edit). (3) **Pull-on-mismatch** — `EvaluatePull` arms `needPull` whenever the live `(currentQueen, heardVersion, currentZone)` ≠ `appliedKey` (inequality, so a queen reload's counter reset still refetches; unknown cold-login zone defers); `FlushSync` fires one coalesced `PR` per tick, and the queen coalesces all `PR`s into one broadcast per zone per tick.
- **Trust + empty semantics.** `OnProfile` auto-applies **only** from the drone's own `Swarm.currentQueen` (stronger than the rank≥1 baseline; split-brain-safe). An **empty** snapshot → **keep** the current plan (a failover to an unprepared queen must not blank drones mid-run while the old queen's marks are still on the mobs) but still record the version. **Known limitation:** an intentional full-zone clear therefore does not propagate (a real re-plan is a non-empty Save, which does).
- **Wire surface:** `P` (profile snapshot, queen→drones) + `PR` (pull-request, drone→queen) message types + the `planVersion` heartbeat field, all in the pure codec (see **Sync Wire Codec**). The decode path is the untrusted parse edge; a focused `/security-review` of PR #75 found no actionable findings (the queen-gated apply + the server-rank `SetRaidTarget` backstop bound it). Off-client specs in `tests/profile_sync_spec.lua` (queen-gating, empty-keeps, the pull predicate, push-on-promotion, **[slice 7]** the `PushProfile` `HR`-emission + `OnHealerRecord` apply) + the codec round-trips/rejections in `sync_codec_spec.lua` (incl. the `HR` type). Harness 159/0.
- **Late-join reliability [v0.29, PRs #95/#98/#100].** A fresh-login joiner pulls and syncs within seconds; the hardening that made it reliable (each fix detailed in the **TankMark_Swarm.lua** file entry): `PR` handled before the rank gate so rank-0 drones can pull at all (#98); the queen keeps beating through an `IsPartyLeader()` blip and a drone doesn't re-bootstrap on a candidacy flicker, so the election settles (#95/#100); `OnProfileSaved` clears `appliedKey` so a locally-reset profile re-pulls (#100, *the* "no sync on join" cause in a reset-without-`/reload` loop); and the pull/render path reads the healed `GetCachedZone` (#100). 2-box in-game verified.

### Manual Handoff
**[v0.29, swarm slice 5a, PRs #78/#79/#80]** A queen voluntarily passes the crown to a named candidate via `/tmark handoff <name>`. **The crown moves only through the deterministic election** — the offer nudges the *claimant set*, never imperatively writing `selfAmQueen`, so the single-queen invariant slice 3 established holds at every instant (the imperative model was rejected — it makes the handshake a second marking authority, reviving the double-queen race). `SWARM_DESIGN.md` §5.10.
- **The split (`AdvertisedClaim`).** `selfAmQueen` stays the election output / marking gate (drives `ShouldDriveMarks`, unchanged). A new pure `AdvertisedClaim(selfAmQueen, pendingClaim, relinquish) = (selfAmQueen or pendingClaim) and not relinquish` is the *claim bit* — what `SendBeat` encodes as `amQueen` and what `ComputePresence` counts as a self-claim. With both flags false it collapses to `selfAmQueen`, so an idle swarm is behavior-identical (the slice-5a.2 dormant introduction, harness-proven before any message could activate it).
- **One wire type (`H;<targetName>`).** Directed offer, broadcast queen→target on the existing transport; only the named target acts. **Confirm rides the target's `Q` heartbeat** (`amQueen=1` via `pendingClaim`); **relinquish rides the queen's** (`amQueen=0`). No ACK, no DB — the heartbeat *is* the confirm. Both accept and relinquish **force an off-cycle beat** so the swap completes in ~1s, not the 5s tick.
- **Receiver (`OnHandoffOffer`) — 4 gates + auto-accept** (models passing raid lead): `IsTrustedSender` (rank≥1, in `HandleSync`) ∧ `sender==currentQueen` ∧ `target==self` ∧ (eligible & not bootstrapping). Accept sets `pendingClaim` (+ forced beat); the election promotes the target once the queen relinquishes.
- **Queen (`Recompute`) — relinquish on hearing the claim.** When the queen *hears* its target claiming (`amQueenHeard[target]`), it sets `relinquish` (drops its own claim for the one election that breaks stickiness) + forces a beat. **Anchor rule:** never relinquish until the target claim is heard, so a lost/ignored offer leaves the queen marking. `relinquish` is a strict **one-shot**, cleared every pass — so a target that vanishes mid-swap can never wedge a re-elected queen into advertising `amQueen=0` while marking (a self-inflicted split-brain).
- **TTLs `CLAIM(20s) > PRESENCE(15s) > OFFER(10s)`** (`ExpireHandoffState`): the target's claim must outlive the queen's presence window, so a queen-DC mid-handoff resolves as the target **inheriting** the crown, not a fallback election. A lost offer self-clears at 10s ("not confirmed — you remain Queen").
- **Review/tests:** new wire surface → focused `/security-review` of PR #80 clean (4 gates sound; no forge/escalation/state-corruption path; bounded by the queen-gated accept + the server-rank `SetRaidTarget` backstop). The pure override + inheritance outcomes are pinned in `tests/swarm_election_spec.lua` (the claimant-set crown-pass walk + the queen-DC inheritance specs); the wall-clock TTL plumbing is runtime-only, 2-box in-game verified. Harness 116/0.

### Promotion UX (slice 5b)
**[v0.29, swarm slice 5b, PRs #83/#84/#86]** The local-only presentation layer over the slice-5a protocol — **no wire, no marking path, no `/security-review`**. Three independent pieces, plus a standalone bug surfaced during testing (the rejoin zone fix, PR #85 — see **Zone Cache** above). `SWARM_DESIGN.md` §5.11.
- **Drone gate (PR #83).** `ApplyProfileEditGate()` read-only's the Team Profiles tab for a drone (the queen is the profile's sole writer — see **Profile Sync** — so a drone's edits would be overwritten on the next push). Keyed on `Swarm.IsRunning() and Swarm.lastRole == "DRONE"` — **both** required: a solo player never runs `Recompute` so `lastRole` stays `nil`, and AND-ing `IsRunning()` stops a torn-down swarm from stranding a now-solo editor on a stale role. Disables every write (Save/Add/Template/Copy/Reset + per-row icon/tank/healer/CC/Delete, incl. the browser-mode zone-delete), makes the tank/healer EditBoxes read-only via `EnableMouse(false)`+`ClearFocus` (1.12 EditBoxes have **no** `:Disable`), and shows a banner; keeps views live (zone dropdown, Manage-Profiles toggle, scroll/list). Re-applied at the tail of both `UpdateProfileList` modes, on the tab `OnShow`, and live from the `Recompute` role-transition seam (`RefreshProfileGateIfVisible`, visibility-guarded). **Folded-in live-refresh:** `RefreshProfileTabForZone` (from `Swarm.OnProfile`) reloads the cache + re-renders the open tab when a queen's push edits the shown zone — without it the drone HUD updated but the open tab stayed stale.
- **Recorder-on-promotion prompt (PR #84).** A *dead-queen interlock*: `ProcessUnit` records-and-returns when `IsRecorderActive` (recording bypasses the queen gate), so a drone running the Flight Recorder who is promoted would become the sole marker but **silently never mark**. On the `OnPromoted` rising edge when recording, `Recompute` calls `PromptRecorderOnPromotion(SelfIsSoleCandidate())`. The `StaticPopup`'s `[Stop Recording]` (default) disables the recorder; `[Keep]` / Escape / X all leave it on and warn loudly (`WarnRecordingQueen`) — the warning fires from **`OnHide` gated by a "did they pick Stop" flag** because *1.12 Escape hides without firing `OnCancel`*, so a silent dismiss still warns. Sole-candidate adds a "no one will mark" notice. **Crown-decline is deliberately OUT of scope** — refusing the crown and re-electing someone else is not a local UX change: a naive local decline (force `selfAmQueen=false`) self-perpetuates a raid-wide dead queen (re-elected every cycle), and a correct one needs candidacy suppression = control-plane work for its own reviewed slice.
- **Handoff trigger UI (PR #86).** The §5.6 dropdown — a TankMark-owned trigger over the existing `/tmark handoff`. A transparent `Button` overlays the `swarmStatus` line, shown by `RenderSwarmLine` **only** for a QUEEN with ≥1 eligible candidate (`Swarm.HandoffCandidates()`, the single-sourced present-minus-self set). Click → `InitHandoffMenu` (a `UIDropDownMenu` of candidates) → `ConfirmHandoff` `StaticPopup` → `Swarm.InitiateHandoff`, which **re-validates** queen-only + eligibility + non-self, so a stale pick just prints a rejection (the slash path stays authoritative). Hover tooltip; `CloseDropDownMenus()` on demotion, scoped to our own menu. Two non-obvious gotchas, both reusable:
  - **Repaint cadence.** The affordance depends on the candidate **set**, which changes *without* a role/queen transition (a rejoined+promoted player starts heartbeating while we stay queen). `Recompute` already computes `present`, so it derives `handoffAvail` and repaints on a `lastHandoffAvail` flip too — otherwise the cue never lit up and handoff was slash-only.
  - **1.12 has no Unicode and FontStrings don't parse `|T` inline textures.** A chevron glyph renders blank and `|T...|t` prints literally — so a text cue can't work. The "clickable" cue is a separate **`Texture` region** instead, using the down-arrow `FauxScrollFrameTemplate` already renders (guaranteed present on the client).

All three are in-game 2-box verified. `SWARM_DESIGN.md` §5.11.

### Mob DB Sharing (slice 6)
**[v0.29, swarm slice 6, PRs #89–#93]** Replaces the old unsolicited Mob DB push (rank-gated, silent auto-overwrite) with a WeakAura/pfQuest-style **advertise → pull → consent** model on the receiver. `SWARM_DESIGN.md` §7. The threat model: the `CHAT_MSG_ADDON` `sender` is unspoofable and the privileged `SetRaidTarget` is server-rank-gated, so defenses only protect *persistent local state* (the Mob DB) — and they live on the receiver.
- **The flow.** A player posts a clickable chat **link** (`PostShareLink(zone)` — `/tmark sync`, the HUD "Share Zone Mob DB" menu, or the Manage Zones per-row Share button; any zone, group-only, no rank needed). A clicker's `SetItemRef` hook fires a directed `SR` pull and arms a **pending-click** `(poster:zone, 15s)`. The poster **coalesces** a click-storm (4s) under a per-zone **cooldown** (10s) into **one** broadcast — `SB(count) → N×M → SE`. Broadcast-only because **Turtle has no addon-`WHISPER`** (verified: "Unknown addon chat type"); coalesce-once makes that the better DoS shape anyway.
- **Consent + security.** A receiver buffers a frame **only while it holds a matching pending-click** (solicited-only), keyed on the **unspoofable `sender`** — never the link's/SB's claimed poster (§7.5), so a forged-name link can't make a poisoned DB look trusted. The frame applies **all-or-nothing** (exact `SB` count). The **trust gate** (account-wide `TankMarkDB.Trust`, Blocked > Trusted > Neutral): Blocked dropped, Trusted auto-imports, Neutral shows the custom confirm. Apply = `CreateSnapshot()` then **full-zone replace**. The whole edge was `/security-review`-clean at 6.4a (no consent bypass, no table-key injection, sender-binding sound).
- **Where it lives.** Codec types in `SyncCodec` (`SB`/`SE`/`SR` + link grammar + widened `M`); the trust model in `Core/TankMark_Trust.lua`; the poster + receiver logic + the single DB-apply edge in `Core/TankMark_Sync.lua` (`HandleSync` split into share plane / control plane); the trust-list UI in the Options tab; the `SetItemRef` hook + import-confirm frame in `TankMark_HUD.lua` (Core has no UI).
- **Build:** 6.1 codec+trust-model (PR #89) → 6.2 trust UI (#90) → 6.3 poster (#91) → 6.4a receiver + `/security-review` (#92) → 6.4b cutover, legacy push removed (#93). Trust resolution is harness-tested (`tests/trust_spec.lua`) and the codec round-trips/rejections in `sync_codec_spec.lua`; the stateful receiver is 2-box in-game verified.

## Adding New Features

### Adding a New Mark Assignment Rule
1. Open `Core/TankMark_Assignment.lua`
2. Add your logic to `GetFreeTankIcon()` or create a new function
3. Call it from `DecideKnownMark()` in `Core/TankMark_Processor.lua` (return an intent — do not apply directly)
4. If the rule affects skull assignment, change the shared `IncumbencyBlocks()` — it single-sources both the decide-path `GovernorBlocks()` and the death-path `DecideSkullSuccessor()` (both in `Core/TankMark_Processor.lua`). The `ReviewSkullState` shell in `Core/TankMark_Death.lua` only builds the snapshot and dispatches the intent, so it rarely needs touching.

### Adding a New Debug Log Category
1. Add `DebugLog()` calls with your category string (e.g., "MYFEATURE")
2. **Always** wrap in `if TankMark.DebugEnabled then ... end`
3. Optionally add a `/tmark debug myfeature` filter in `TankMark.lua` `SlashHandler`

### Adding a New Config Tab
1. Create a subdirectory under `UI/Config/` matching the tab's domain (e.g., `UI/Config/NewFeature/`)
2. Create `TankMark_Config_NewFeature.lua` (state + logic) and `TankMark_Config_NewFeature_UI.lua` (UI construction)
3. In the UI file, use private `local function` section builders for each visual region; keep the public entry point to a single `TankMark:CreateNewFeatureTab(parent)` function
4. Set the container frame to `TOPLEFT 0,0 / BOTTOMRIGHT 0,0` to share the coordinate space with existing tabs
5. Register the tab in `UI/TankMark_Options.lua`
6. Update `TankMark.toc` load order

### Adding a New Localized API Call
1. Add the reference to `TankMark.Locals` in `TankMark.lua` (e.g., `_NewFunction = NewFunction`)
2. Use `L._NewFunction(...)` in your code
3. Never call WoW API globals directly outside of `TankMark.lua`

## Coding Standards
- **Localization:** Always use `TankMark.Locals` for WoW API functions (no direct global calls). Use `local L = TankMark.Locals` at the top of each file.
- **Nil safety:** Validate all inputs — check for nil before indexing tables.
- **Table length:** Use `L._tgetn()` (aliased `table.getn()`) for array length. Never use `#table` (not available in Lua 5.0).
- **Version tags:** Comment complex algorithms with `-- [v0.XX]` version tags.
- **Debug guards:** Wrap all `TankMark:DebugLog()` calls in `if TankMark.DebugEnabled then ... end` to avoid argument construction overhead on the hot path.
- **String functions:** Use `L._gfind()` (not `string.find` or `sgfind`) for pattern iteration. Use `L._strfind()` for single match.
- **Type checks:** Use `L._type()` instead of bare `type()`.
- **Global function references:** Use `L._SendChatMessage()`, `L._SetRaidTarget()`, `L._PlaySound()`, `L._CreateFrame()`, etc. — all WoW API calls must go through the Locals table.
- **UI file structure:** Config tab UI files must use private `local function` section builders. No UI construction code inside the public entry point beyond calling section builders and returning the container frame. See `UI/Config/Database/TankMark_Config_Mobs_UI.lua` and `UI/Config/Profiles/TankMark_Config_Profiles_UI.lua` as the canonical patterns.

## Testing (v0.28)

TankMark has an **off-client unit-test suite** for the decision layer. It runs
outside the WoW client — no `/reload` needed — because the decision layer
(`DecideMark` and its helpers in `Core/TankMark_Processor.lua`) reads the world
only through an injected **board** of ports (roadmap #2 Tier 2). Production wires
`TankMark.LiveBoard` (closures over the live methods); tests inject a mock board.

**Run it** (from the repo root):
```
lua5.1 tests/run.lua
```
Exits non-zero if any assertion fails. The harness needs only Lua itself — no WoW
client, and no mock of the `Locals` table: the system under test is zero-global
apart from a few pure-language utilities the harness shims to their stock Lua
versions — `L._tgetn` (→ `table.getn`) for the decide layer, and **[v0.29]**
`L._sub` / `L._strfind` / `L._tonumber` / `L._tinsert` / `L._pairs` / `L._ipairs`
and **[slice 4]** `L._gfind` (→ `string.gmatch`, Vanilla's `string.gfind`) for the
SyncCodec and the swarm, and **[v0.31]** `L._type` (→ `type`) for the ZoneView validator.

**Runtime:** production code stays Lua **5.0**-idiomatic (Vanilla WoW); the harness
runs on Lua **5.1** (closest widely-available interpreter — it still has
`table.getn`, removed in 5.2+). Do not introduce 5.1-only idioms into shipped code.

**Layout** (`tests/`, dev-only — never shipped to the AddOns folder; the deploy
script excludes the whole tree by path prefix):
- `run.lua` — entry point; lists the spec files and prints a pass/fail summary.
- `support/harness.lua` — stubs the minimum, loads the SUT (`Assignment.lua`,
  `Processor.lua`, and **[v0.29]** `SyncCodec.lua` + `Swarm.lua`, **[v0.31]** plus
  the pure `Reservation.lua` + `ZoneView.lua` seams — all
  definition-only), and provides a tiny `describe`/`it`/`eq`/`eq_intent` runner (**[v0.31]** plus `eq_skull` for `DecideSkullSuccessor`'s `action`-tagged intents).
- `support/board.lua` — `make_board(overrides)`, a mock ports board with safe
  defaults (in combat, nothing busy/disabled/free, no CC, no blocker).
- `*_spec.lua` — `incumbency_spec` (the pure `IncumbencyBlocks` predicate),
  `decide_mark_spec` (combat gate, selection, fallback, bails), `governor_spec`
  (skull governor, CC, the `allowSteal` asymmetry), **[v0.29]** `sync_codec_spec`
  (the pure `SyncCodec` encode/decode round-trip + every rejection rule, incl. the
  slice-4 `P`/`PR`/`planVersion` types, **[slice 5a.1] the `H` handoff type**, **[slice 6]** the `SB`/`SE`/`SR` share-frame types + the `tankmark:` chat-link, and **[slice 7]** the `HR` healer record),
  `swarm_election_spec` (the pure election core —
  `DeterministicMax`/`ElectQueen`/`ComputePresence`/`DeriveRole`, **plus [slice 5a]
  `AdvertisedClaim` + the claim-set crown-pass walk + the queen-DC inheritance
  outcomes**), and **[slice 4]** `profile_sync_spec` (queen-gated apply, empty-keeps,
  the `EvaluatePull` triple predicate, push-on-promotion, **and [slice 7]** the
  `PushProfile` `HR`-emission ordering + the `OnHealerRecord` apply/gating), **[v0.30, marking Phase 3]** `role_prio_spec` (the pure `RoleTierPrio` role×tier → default-prio table, incl. the nil/unknown-role → MELEE and nil/unknown-tier → `normal` degrades), and **[v0.30, marking Phase 4]** `cc_worthiness_spec` (the `CCWorthiness` curve + `CCTierEligible` + the `MeetsAutoCCFloor` threshold), `pull_assignment_spec` (`DecidePull` kill-first / prio-ordered CC selection, reserve-a-kill-target, floor-gating, legality, determinism), the `IsCCSlotMark` cases in `legal_cc_spec`, and the Phase-4 `ResolveCC` auto-CC + reserve cases in `governor_spec`. **[v0.31]** `mark_normals_spec` locks the `MarkNormalsEnabled` default-true persistence accessor (nil/unset → true, explicit false → false), and **[v0.31, candidate A]** `decide_skull_successor_spec` pins every branch of the pure death-path `DecideSkullSuccessor` (mark8-alive confirm/adopt/stale, dup-event, sequential, no-candidate, and the `>=` incumbency boundary → assign).

**Adding a spec:** create `tests/<name>_spec.lua` using `describe`/`it`/`eq`/
`eq_intent` and `make_board{...}`, then add it to the `SPECS` list in `run.lua`.

## SavedVariables

| Variable | Scope | Description |
|---|---|---|
| `TankMarkDB` | Account-wide | Mob database (`Zones`), debug log (`DebugLog`), Mob DB sharing trust axis (`Trust`, name → `blocked`/`trusted`; **[v0.29, slice 6]**) |
| `TankMarkProfileDB` | Per-character | Team profiles (mark → tank → healers assignments per zone) |
| `TankMarkDB_Snapshot` | Per-character | Up to 3 database snapshots for corruption recovery |
| `TankMarkCharConfig` | Per-character | Character-specific UI settings (HUD position, etc.) |

## Event Flow (Simplified)